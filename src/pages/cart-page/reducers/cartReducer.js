import Item1 from '../../images/shoe1.jpg'
import Item2 from '../../images/shoe2.jpg'
import Item3 from '../../images/shoe3.jpg'
import Item4 from '../../images/shoe4.jpg'
import Item5 from '../../images/shoe5.jpg'
import Item6 from '../../images/shoe6.jpg'
import { ADD_TO_CART,REMOVE_ITEM,SUB_QUANTITY,ADD_QUANTITY,ADD_SHIPPING } from '../actions/action-types/cart-actions'


const initState = {
    items: [
        {id:1,title:'Winter body', desc: "Take off with responsiveness, touch down with control, ride with comfort..", price:123,img:Item1},
        {id:2,title:'Adidas', desc: "Neutral shoes for the ultimate running experience.", price:78,img: Item2},
        {id:3,title:'Vans', desc: "Boost is our most responsive cushioning ever: The more energy you give, the more you get",price:187,img: Item3},
        {id:4,title:'White', desc: "Fitcounter moulded heel counter provides a natural fit that allows optimal movement of the Achilles", price:157,img:Item4},
        {id:5,title:'Cropped-sho', desc: "Stretchweb outsole flexes naturally for an energised ride, while Continental™ Rubber gives you superior traction", price:130,img: Item5},
        {id:6,title:'Blues', desc: "RUNNING SHOES WITH A LIGHTWEIGHT UPPER AND RESPONSIVE CUSHIONING",price:88,img: Item6}
    ],
    addedItems:[],
    total: 0

}
const cartReducer= (state = initState,action)=>{
   
    //INSIDE HOME COMPONENT
    if(action.type === ADD_TO_CART){
          let addedItem = state.items.find(item=> item.id === action.id)
          //check if the action id exists in the addedItems
         let existed_item= state.addedItems.find(item=> action.id === item.id)
         if(existed_item)
         {
            addedItem.quantity += 1 
             return{
                ...state,
                 total: state.total + addedItem.price 
                  }
        }
         else{
            addedItem.quantity = 1;
            //calculating the total
            let newTotal = state.total + addedItem.price 
            
            return{
                ...state,
                addedItems: [...state.addedItems, addedItem],
                total : newTotal
            }
            
        }
    }
    if(action.type === REMOVE_ITEM){
        let itemToRemove= state.addedItems.find(item=> action.id === item.id)
        let new_items = state.addedItems.filter(item=> action.id !== item.id)
        
        //calculating the total
        let newTotal = state.total - (itemToRemove.price * itemToRemove.quantity )
        console.log(itemToRemove)
        return{
            ...state,
            addedItems: new_items,
            total: newTotal
        }
    }
    //INSIDE CART COMPONENT
    if(action.type=== ADD_QUANTITY){
        let addedItem = state.items.find(item=> item.id === action.id)
          addedItem.quantity += 1 
          let newTotal = state.total + addedItem.price
          return{
              ...state,
              total: newTotal
          }
    }
    if(action.type=== SUB_QUANTITY){  
        let addedItem = state.items.find(item=> item.id === action.id) 
        //if the qt == 0 then it should be removed
        if(addedItem.quantity === 1){
            let new_items = state.addedItems.filter(item=>item.id !== action.id)
            let newTotal = state.total - addedItem.price
            return{
                ...state,
                addedItems: new_items,
                total: newTotal
            }
        }
        else {
            addedItem.quantity -= 1
            let newTotal = state.total - addedItem.price
            return{
                ...state,
                total: newTotal
            }
        }
        
    }

    if(action.type=== ADD_SHIPPING){
          return{
              ...state,
              total: state.total + 6
          }
    }

    if(action.type=== 'SUB_SHIPPING'){
        return{
            ...state,
            total: state.total - 6
        }
  }
    
  else{
    return state
    }
    
}

export default cartReducer
